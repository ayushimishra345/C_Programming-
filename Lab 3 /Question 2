/* WAP a C program to Convert temperature from Celsius to 
Fahrenheit using the formula: F = (C * 9/5) + 32. */

#include<stdio.h>
int main()
{
  float F,C;
  printf("Enter Temperature In Celcius:");
  scanf("%f",&C);
  F=(C*9/5)+32;
  printf("Temperature In Fahrenheit:%.2f\n",F);
  return 0;
  }
